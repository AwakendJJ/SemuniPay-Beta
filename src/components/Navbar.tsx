import React from 'react';

// This component is no longer needed for the simple landing page
// Keeping it empty to avoid breaking imports
const Navbar = () => {
  return null;
};

export default Navbar;